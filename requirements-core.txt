# Core dependencies — LOW ROCm risk, all pure-Python / CPU or plain-PyTorch.
# NOTE: torch / torchvision are intentionally NOT pinned here. On Exea the base
# image is expected to be rocm/pytorch (ROCm build of PyTorch already present).
# Reinstalling torch would risk clobbering the ROCm build with a CPU/CUDA wheel.
# setup.sh checks that torch imports and reports the build; it does not force it.
#
# numpy is pinned <2 because cell-gears/GEARS assume the numpy 1.x ABI.
numpy==1.26.4
scipy>=1.10,<1.15
pandas>=2.0,<2.3
scikit-learn>=1.3,<1.6
scanpy>=1.9,<1.11
anndata>=0.9,<0.12
transformers>=4.38,<4.50
timm>=0.9,<1.1
einops>=0.7
peft>=0.10,<0.14          # HF PEFT for BehaviorFM Tier-B LoRA-on-backbone / IA3 (pure-Python, ROCm-safe)
huggingface_hub>=0.20,<0.30
tqdm>=4.65
requests>=2.28          # explicit: behaviorfm AP-10K Google-Drive download uses it
